<template>
  <header>
      <div class="logoBox">
          <img src="./source/logo.png" class="logo" alt="">
          <a class="cancelBtn" v-if="step === 2 || step === 3"
            v-on:click="goHomeHeader">Cancel</a>
          <a class="nextBtn" v-if="step === 2" v-on:click="step++">Next</a>
          <a class="doneBtn" v-if="step === 3">Done</a>
      </div>
  </header>
</template>

<script>
export default {
    props: ['step'],
    methods: {
        goHomeHeader: function(){
            this.$emit('goHomeHeader');
        },
    }
}
</script>

<style>
header{
    display: block;
    background-color: #fafafa;
    width: 375px;
    height: 50px;
    position: fixed; 
    z-index: 999;
    border-bottom: 1px solid #eee;
}
.logoBox{
   position: relative;
}
.logo{
     position: absolute; left: 50%; top: 12px;
    transform: translate(-50%, 0);
   max-width: 120px;
   display: block;
}
.cancelBtn, .nextBtn, .doneBtn{
    position: absolute; top: 12px;
    color: #209cee;
    font-weight: bold;
    cursor: pointer;
}
.cancelBtn{
    left: 10px;
}
.nextBtn, .doneBtn{
    right: 10px;
}
</style>